# Executing this script will gather the trimmed, undervalued industry group from its
# JSON file. User will select:
# 1) The cash amount they'd like to invest
# 2) A portfolio style (risky, moderate, safe) and
# 3) A point along the efficient frontier (highest return, optimal, lowest risk).
# The script will create a corresponding weighted portfolio and print buy orders.