# Executing this script will gather the trimmed, undervalued industry group from its
# JSON file. User will select 1) A portfolio style (risky, moderate, safe) and
# 2) A point along the efficient frontier (highest return, optimal, lowest risk).
# The script will create a corresponding portfolio and print buy orders.